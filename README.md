# 📘 Project Setup Guide

این فایل توضیح می‌دهد چگونه پروژه را نصب، اجرا و سرویس‌های جانبی مثل صف‌ها
و Ngrok را راه‌اندازی کنید.

------------------------------------------------------------------------

## 🚀 1. Clone & Install

``` bash
git clone <repository-url>
cd <project-folder>
```

### Install Dependencies

``` bash
composer install
npm install
```

------------------------------------------------------------------------

## ⚙️ 2. Environment Setup

یک کپی از فایل env نمونه بسازید:

``` bash
cp .env.example .env
```

کلید اپلیکیشن لاراول:

``` bash
php artisan key:generate
```

تنظیمات دیتابیس را داخل `.env` انجام دهید و سپس:

``` bash
php artisan migrate --seed
```

------------------------------------------------------------------------

## 🖥️ 3. Running the Application

برای اجرای پروژه:

``` bash
php artisan serve
```

پروژه پیش‌فرض در آدرس زیر بالا می‌آید:

    http://127.0.0.1:8000

------------------------------------------------------------------------

## 📩 4. Queue Worker (برای پردازش سفارش‌ها)

این پروژه از **Queue** برای مدیریت رویداد ایجاد سفارش استفاده می‌کند.\
برای اجرای صف:

``` bash
php artisan queue:work --queue=orderCreated
```

------------------------------------------------------------------------

## 🌍 5. Ngrok (برای Webhook و تست خارجی)

برای اینکه سرور محلی شما از بیرون قابل دسترس باشد (مثلاً برای وبهوک‌ها)،
از **ngrok** استفاده شده است.

### نصب Ngrok

``` bash
brew install ngrok
```

### اجرای Ngrok روی پورت 8000

``` bash
ngrok http 8000
```

پس از اجرا، Ngrok یک لینک عمومی مشابه زیر به شما می‌دهد:

    https://f3c1-192-168-1-10.ngrok-free.app

این لینک را می‌توانید در پیکربندی وبهوک‌ها، APIها یا سرویس‌های خارجی قرار
دهید.

------------------------------------------------------------------------

## 🔧 6. نکات مهم

-   اگر وبهوک‌ها درست عمل نمی‌کنند، مطمئن شوید queue worker اجرا شده باشد.
-   در صورت تغییر آدرس Ngrok، تنظیمات وبهوک باید به‌روز شود.
-   برای خطایابی queue:

``` bash
php artisan queue:failed
